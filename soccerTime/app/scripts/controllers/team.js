use strict';

/**
 * @ngdoc function
 * @name soccerTimeApp.controller:TeamCtrl
 * @description
 * # TeamCtrl
 * Controller of the soccerTimeApp
 */
angular.module('soccerTimeApp')
  .controller('TeamCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
